---
title: Documentation
nav_order: 0
---

# Documentation

This site documents **itself**. The API reference below is generated from this host's own routes, not
copied from an upstream spec — so what you read here is what this deployment actually serves.

<SiteNav rootPath="/docs" maxDepth={3} />

## Reference surfaces

Every installed beam package that has a reference surface to contribute seeds its own page beneath this
one. Uninstall a package and its page stays — it is a row this site owns — showing an empty state rather
than vanishing.

---

_Seeded by `splicewire/laravel-beam-ux`. This page is a `BeamUxEntry` row like any other: edit it, move
it, or re-root the whole `/docs` subtree by changing this entry's `segment`. Nothing re-asserts it._
